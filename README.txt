
Lead Cross-Reference Tool — Starter Files (Imperfect rename)
===========================================================

This version enforces:
- Exactly **two** CSV uploads
- A single join type: **inner** (matches-only)
- Optional **Imperfect matching** with a score threshold

Files included:
- app.py
- requirements.txt

Quick start (after unzipping):
1) Optional: create a virtual env
2) pip install -r requirements.txt
3) streamlit run app.py
